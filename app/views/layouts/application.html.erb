<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>

  <body>
      
      <!-- conditional for user sessions/rendering links -->
      <% if @current_user != nil %>

      <a href="/">Your todo list</a>
      |
      <a href="/edit_user_profile">Edit profile</a>
      |
      <a href="/user_sign_out">Sign Out</a>
    
      <% else %>

      <a href="/user_sign_in">Sign In</a>
      |
      <a href="/user_sign_up">Sign up</a>
      </li>
      <% end %>

    <p style="color: green"><%= notice %></p> <!-- notice and alert showing in all the pages -->
    <p style="color: red"><%= alert %></p>

    <%= yield %>
  </body>
</html>
